/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['files.edgestore.dev', 'hebbkx1anhila5yf.public.blob.vercel-storage.com','static.wikia.nocookie.net'],
  },
};

export default nextConfig;
